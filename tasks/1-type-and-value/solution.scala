object Task extends App {
  val res0: Int = 1 + 2 // res0 = 3
  val res1: Int = "3".toInt // res1 = 3

  /* The type of "foo".toInt expression is Int.
    But the evaluation of the expression fails. */
}
